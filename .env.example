# Mapbox access token for satellite imagery and maps.
# Get a token at https://account.mapbox.com/
# Add this file as .env with your actual token before running the app.
#
# The Node server (FAA MTR backend) reads this same variable to proxy Mapbox Static Images
# for PDF export — browser fetch() to api.mapbox.com is usually blocked by CORS.
# Use: npm run dev:all   (or run `npm run server` in one terminal and `npm run dev` in another)
# so /api/mapbox-static is available when you export the report from the web app.
# Copy this file to `.env` and replace the placeholder with your real token (never commit `.env`).
VITE_MAPBOX_ACCESS_TOKEN=

# Optional: public URL of the Node API when the web app is hosted without a same-origin proxy
# (e.g. GitHub Pages). Leave empty locally — Vite proxies /api to port 3001.
# VITE_API_BASE_URL=https://your-api.example.com

# --- Survey map overlay: USDA NAIP (no keys required) ---
# GET /api/recent-imagery proxies public NAIP ImageServer exportImage. Requires npm run dev:all
# or a deployed API with outbound HTTPS to naip.imagery1.arcgis.com / gis.apfo.usda.gov.
# Legacy Copernicus Sentinel-2 (unused): CDSE_OAUTH_CLIENT_ID / CDSE_OAUTH_CLIENT_SECRET

# --- Server: ForeFlight Content Pack API (SQLite + ZIP blobs on disk) ---
# Optional. When set, all /api/content-packs/* routes require:
#   Authorization: Bearer <key>   or   X-API-Key: <key>
# CONTENT_PACK_API_KEY=your-secret-key
# Where to store SQLite DB and baseline ZIPs (default: ./data/content-packs)
# CONTENT_PACK_DATA_DIR=./data/content-packs

# --- Server: Wing Administrator PIN (gates pack lifecycle: upload, create-empty, future rename/delete) ---
# Required for any admin-only content-pack operation to function. When unset,
# admin routes return 503 "admin_not_configured".
#
# Single-admin form (simplest):
# CONTENT_PACK_ADMIN_PIN=4815
#
# Multi-admin form (JSON array; future-proofs multiple Wings / multiple admins):
# CONTENT_PACK_ADMIN_PINS=[{"pin":"4815","name":"Greg D.","wing":"NM"},{"pin":"9023","name":"Pat L.","wing":"AZ"}]
#
# Optional: HMAC secret used to sign admin session tokens. If unset, the server
# generates an ephemeral secret on each startup (admin tokens then invalidate
# on restart, which is fine for low-traffic internal admin consoles). Set this
# in production if you want admin sessions to survive restarts.
# CONTENT_PACK_ADMIN_SECRET=at-least-16-chars-of-random-bytes

# --- Client: optional static API key for dev (prefer browser localStorage key in UI) ---
# VITE_CONTENT_PACK_API_KEY=
